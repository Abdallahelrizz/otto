DATABASE_URL=postgresql://otto:otto_dev@localhost:5432/otto
REDIS_URL=redis://localhost:6379
PISTON_URL=http://localhost:2000
OTTO_FILES_DIR=./files
CREDENTIAL_ENCRYPTION_KEY=replace_with_64_hex_chars_from_openssl_rand_hex_32
SESSION_DAYS=7
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_SESSION_TOKEN=
S3_REGION=us-east-1
S3_ENDPOINT=
S3_BUCKET=
PORT=3000
HOST=0.0.0.0
WORKER_CONCURRENCY=10  # BullMQ worker concurrency
EXECUTION_RETENTION_DAYS=30  # Days to keep execution records (default: 30)
API_RATE_LIMIT=1000    # Per-user API requests per minute (default: 1000)
EXEC_RATE_LIMIT=100    # Per-workspace executions per minute (default: 100)
# Set to true only on self-hosted deployments that intentionally call internal microservices from workflows
SSRF_ALLOW_PRIVATE=false
