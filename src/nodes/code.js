// Code node — sandbox runtime not yet decided (isolated-vm vs vm2 vs Judge0)
// The handler slot is registered so imports and the n8n importer don't break.
export async function codeNode() {
  throw new Error('Code node: sandbox runtime is not yet implemented. See project plan for decision.');
}
